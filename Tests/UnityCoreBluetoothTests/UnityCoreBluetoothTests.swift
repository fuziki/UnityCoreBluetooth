    import XCTest
    @testable import UnityCoreBluetooth

    final class UnityCoreBluetoothTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(UnityCoreBluetooth().text, "Hello, World!")
        }
    }
